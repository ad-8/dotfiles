# remap prefix key
# TODO testing ö - next alternative would be C-a (best with capslock remapped to ctrl)
unbind C-b
set -g prefix ö
bind ö send-prefix

# reload config file -- \; is just escape ;
bind r source-file ~/.tmux.conf \; display "Config reloaded!"

# bind = needs prefix; bind -n = no prefix
# switch panes using Alt-arrow without prefix
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# These require prefix (e.g. Ctrl-b or your custom prefix)
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# split panes using | and -
bind | split-window -h
bind - split-window -v
#unbind '"'
#unbind %


bind ä display "keira"
